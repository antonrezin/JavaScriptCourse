let array = ["Milk", "Bread", "Juice", "Eggs", "Butter", "Salt", "Oranges", "Apples"];

function sortArray() {
    for (let i = 0; i < array.length; i++) {
    }
    array.sort();
    array.reverse();
    console(array);
}